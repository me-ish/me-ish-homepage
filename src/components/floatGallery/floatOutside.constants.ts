/**
 * Float Gallery 外世界（雲海・遠景光）の設定
 *
 * 上品で控えめな雰囲気を維持
 */

export const FLOAT_OUTSIDE = {
  // ============================================================
  // 背景・フォグ
  // ============================================================
  background: {
    color: '#0d1117', // 深いブルーグレー（内装と調和）
  },

  fog: {
    color: '#0d1117',
    near: 60,   // 内装に影響しない距離から開始
    far: 300,   // 外の粗を隠す
  },

  // ============================================================
  // 雲海レイヤー
  // ============================================================
  clouds: {
    // 共通
    baseY: -15,        // ギャラリー床（y=0）より十分下
    size: 400,         // 十分大きく端が見えないように

    // レイヤー別設定
    layers: [
      { yOffset: 0, opacity: 0.12, speed: 0.003, scale: 1.0, color: '#1a2030' },
      { yOffset: -3, opacity: 0.08, speed: 0.002, scale: 1.2, color: '#151b28' },
      { yOffset: -6, opacity: 0.05, speed: 0.001, scale: 1.5, color: '#101520' },
    ] as const,
  },

  // ============================================================
  // 遠景の光（ビーコン / 遠い街の灯り）
  // ============================================================
  distantLights: {
    count: 120,            // 過剰にしない
    radiusMin: 140,        // ギャラリーから十分離す
    radiusMax: 260,
    yMin: -5,
    yMax: 25,
    size: 2.5,             // 小さめ
    opacity: 0.6,          // 控えめ
    color: '#b8c4d8',      // 青白い光
  },

  // ============================================================
  // 水平線のグロウ（遠くの光の集積）
  // ============================================================
  horizonGlow: {
    enabled: true,
    y: -8,
    radius: 200,
    width: 400,
    height: 15,
    opacity: 0.04,
    color: '#2a3545',
  },
} as const;
