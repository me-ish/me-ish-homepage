# Remote migration recovery: `20260214050324`

調査日: 2026-07-23（JST）
対象project ref: `lvnfspyainrxtztjytbo`

| version | remote name | source | original SQL recovered | checksum SHA-256 | created objects | current DB equivalent | confidence | recommended treatment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `20260214050324` | `create_card_requests_table` | `supabase_migrations.schema_migrations.statements` | yes（保存されていたstatement列を順序どおり回収） | `83fac13127a40aa9545a486836317b3ba4fd76ecaa3c86a5d13576b3910cffe1` | `public.card_requests`、PK、3 index | yes。全19列の型・NULL・default、PK、3 indexを比較。RLS disabledも回収SQLがRLSを設定しないことと一致 | high | `restore` |

## 回収方法とchecksum

- 第一優先ソースである `schema_migrations.statements` に1要素が残っていたため、GitやDB再構成へフォールバックしていない。
- checksumは、statement配列を順序どおりLF 1文字で連結したUTF-8バイト列に対するSHA-256である。
- これは履歴テーブルに保存されたSQL statement本文のchecksumである。失われた元ファイルのコメント、statement終端のセミコロン、改行コード、末尾空白まで含むbyte-for-byteのファイルchecksumではない。
- 本文は `reconstructed_from_database` ではない。

## Recovered SQL

```sql

CREATE TABLE card_requests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'draft',
  payload JSONB,
  design JSONB,
  content JSONB,
  public_id UUID,
  public_slug TEXT,
  visibility TEXT DEFAULT 'private',
  published_at TIMESTAMPTZ,
  tier TEXT DEFAULT 'free',
  payment_status TEXT DEFAULT 'unpaid',
  stripe_session_id TEXT,
  paid_at TIMESTAMPTZ,
  session_token TEXT NOT NULL,
  renderer_version TEXT DEFAULT '1.0.0',
  error TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_card_requests_email ON card_requests (email);
CREATE INDEX idx_card_requests_public_slug ON card_requests (public_slug);
CREATE INDEX idx_card_requests_public_id ON card_requests (public_id);
```

## 現行DBとの比較

- table: `public.card_requests` が存在する。
- column: 19列の型、NULL可否、defaultが回収SQLと一致する。
- constraint: `id`のPRIMARY KEYが一致する。回収SQLにCHECK、FK、UNIQUEはない。
- index: `idx_card_requests_email`、`idx_card_requests_public_slug`、`idx_card_requests_public_id`が、column順を含めて一致する。
- RLS/policy: RLS disabled、policyなし。回収SQLもRLS/policyを定義していない。
- grant: 回収SQLはgrantを指定していないため、現行のanon/authenticatedに対する全table privilegeはこのSQLとの同値判定対象外であり、別のセキュリティ課題として扱う。

## 使用禁止

このコードブロックは回収証跡であり、migrationファイルへのコピー、production適用、history repairの根拠単独として使用してはならない。baseline dry-runでschema全体とACLを再検証する。
