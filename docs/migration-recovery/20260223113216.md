# Remote migration recovery: `20260223113216`

調査日: 2026-07-23（JST）
対象project ref: `lvnfspyainrxtztjytbo`

| version | remote name | source | original SQL recovered | checksum SHA-256 | created objects | current DB equivalent | confidence | recommended treatment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `20260223113216` | `create_aura_projects` | `supabase_migrations.schema_migrations.statements` | yes（保存されていたstatement列を順序どおり回収） | `0da2f8a82bc108715591b34fbbfba77f7d834dfb09daba099acfda464ee7f57b` | `public.aura_projects`、PK、`public_slug` UNIQUE | yes（後続`20260225122344`の2列を除いたbase定義）。型・NULL・default、PK、UNIQUEを比較 | high | `restore` |

## 回収方法とchecksum

- 第一優先ソースである `schema_migrations.statements` に1要素が残っていたため、GitやDB再構成へフォールバックしていない。
- checksumは、statement配列を順序どおりLF 1文字で連結したUTF-8バイト列に対するSHA-256である。
- 元ファイルのコメント、セミコロン、改行コード、末尾空白を含むbyte-for-byteのファイルchecksumではない。
- 本文は `reconstructed_from_database` ではない。

## Recovered SQL

```sql
CREATE TABLE aura_projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_token UUID NOT NULL,
  email TEXT,

  -- コンテンツ
  name TEXT,
  display_title TEXT,
  tagline TEXT,
  bio TEXT,
  avatar_path TEXT,

  -- デザイン
  theme_id TEXT NOT NULL DEFAULT 'pure',
  accent_color TEXT DEFAULT '#00a1e9',
  font_preset TEXT DEFAULT 'cleanJa',
  layout_pref TEXT DEFAULT 'center',

  -- 構造化コンテンツ
  works JSONB NOT NULL DEFAULT '[]',
  services JSONB NOT NULL DEFAULT '[]',
  skills JSONB NOT NULL DEFAULT '[]',
  social JSONB NOT NULL DEFAULT '{}',

  -- セクション管理
  section_order TEXT[] DEFAULT ARRAY['hero','works','skills','services','contact'],
  section_visibility JSONB NOT NULL DEFAULT '{}',

  -- 公開
  status TEXT NOT NULL DEFAULT 'draft',
  public_id UUID DEFAULT gen_random_uuid(),
  public_slug TEXT UNIQUE,
  visibility TEXT NOT NULL DEFAULT 'private',
  published_at TIMESTAMPTZ,
  payment_status TEXT NOT NULL DEFAULT 'unpaid',

  -- メタ
  renderer_version TEXT NOT NULL DEFAULT '2.0.0',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

## 現行DBとの比較

- table: `public.aura_projects` が存在する。
- column: 回収SQLのbase 27列について型、NULL可否、defaultが一致する。現行DBには後続versionで追加された`avatar_shape`と`bg_pattern`も存在する。
- constraint: `id` PRIMARY KEYと`public_slug` UNIQUEが一致する。回収SQLにCHECK/FKはない。
- index: UNIQUE constraintに対応するindexが存在する。
- RLS/policy: RLS disabled、policyなし。回収SQLもRLS/policyを定義していない。
- grant: 回収SQLはgrantを指定していない。現行anon/authenticated privilegeは別のセキュリティ課題として扱う。

## 使用禁止

このコードブロックは回収証跡であり、production適用やmigrationファイル作成には使用しない。base定義と後続2列を一体としてbaseline dry-runで検証する。
