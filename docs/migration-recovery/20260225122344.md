# Remote migration recovery: `20260225122344`

調査日: 2026-07-23（JST）
対象project ref: `lvnfspyainrxtztjytbo`

| version | remote name | source | original SQL recovered | checksum SHA-256 | created objects | current DB equivalent | confidence | recommended treatment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `20260225122344` | `add_avatar_shape_bg_pattern_to_aura_projects` | `supabase_migrations.schema_migrations.statements` | yes（保存されていたstatement列を順序どおり回収） | `4018aba6830a50959af86c7b3eb793307ef761a71d955ce00736a0d7867a7f01` | `public.aura_projects.avatar_shape`、`bg_pattern` | yes。両列の型、NOT NULL、defaultを比較 | high | `restore` |

## 回収方法とchecksum

- 第一優先ソースである `schema_migrations.statements` に1要素が残っていたため、GitやDB再構成へフォールバックしていない。
- checksumは、statement配列を順序どおりLF 1文字で連結したUTF-8バイト列に対するSHA-256である。
- 元ファイル全体のbyte-for-byte checksumではない。
- 本文は `reconstructed_from_database` ではない。

## Recovered SQL

```sql

ALTER TABLE aura_projects
  ADD COLUMN IF NOT EXISTS avatar_shape text NOT NULL DEFAULT 'circle',
  ADD COLUMN IF NOT EXISTS bg_pattern   text NOT NULL DEFAULT 'none';
```

## 現行DBとの比較

- `public.aura_projects.avatar_shape`: `text NOT NULL DEFAULT 'circle'`で一致する。
- `public.aura_projects.bg_pattern`: `text NOT NULL DEFAULT 'none'`で一致する。
- table本体のbase定義は`20260223113216`の回収記録を参照する。

## 使用禁止

このコードブロックは回収証跡であり、production適用やmigrationファイル作成には使用しない。`IF NOT EXISTS`だけを根拠に安全な再適用とは判定しない。
