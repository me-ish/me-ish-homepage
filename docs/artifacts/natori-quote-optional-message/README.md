# Hero・見積もりフォームの追加修正

今回分はローカルのみ。本番DBへの適用・アプリのデプロイは未実施。

- Heroの通常納期・見積もり返信目安の2文を削除。
- 量産イラストは「表情指定 → 表情差分 → 衣装カラーチェンジ → 商用利用」の順に変更。
- 見積もり時の「ご相談・ご依頼の内容」は任意。相談時は必須。量産イラストの表情指定は引き続き必須。
- 自由記述なしの見積もりを受け付けるため、アプリとDBの入力検証を合わせて変更。

## 本番への反映

アプリのデプロイ前に、`supabase/migrations/20260908111337_etorie_quote_optional_message.sql` を本番DBへ適用してください。アプリだけ更新した場合、詳細が空の見積もりがDBで拒否されます。

SQLは既存の検証関数を置き換え、自由記述の最低1項目チェックを相談時だけにします。その他の検証、既存の権限、関数の呼び出し元、保存済みのデータは維持します。

## 検証

- 関連5ファイル・134テスト成功（Hero、フォーム、入力データ、検証スキーマ、受付API）。APIのメールとDBはモック。
- TypeScript: `tsc --noEmit --incremental false` 成功。
- 一時Postgres環境（PGlite 0.5.8）で8チェック成功。変更前の空欄拒否、変更後の見積もり空欄許可、相談空欄拒否、相談入力あり許可、不明モード・文字数超過・不完全なその他指定の拒否、再適用を検証。
- 390px / 1440pxのローカルデモで、オプションのDOM順序、見積もり内容欄の任意化、相談へ戻したときの必須化、空欄での見積もりデモ送信、横スクロールなしを確認。ページ例外・コンソールエラーなし。
- スクリーンショットはエトリエデモのサンプル表示。本番サイトそのものの画像ではありません。

手動確認は、Hero → 見積もり希望 → 量産イラスト → デザイン・表情指定を入力 → 内容欄を空のまま送信、の順で行えます。デモで送信確認できます。本番からの実メール送信は実施していません。

変更箇所と正確な置換ブロックは `changes.patch`、DB変更は上記SQLを参照してください。

実装時の参照: [Supabase Database Functions](https://supabase.com/docs/guides/database/functions)
