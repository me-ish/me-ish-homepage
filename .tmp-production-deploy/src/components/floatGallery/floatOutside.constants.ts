/**
 * Float Gallery 外世界（雲海・遠景光）の設定
 *
 * 雲海の上に浮かぶ美術館。遠景ライトで奥行き感を演出。
 */

export const FLOAT_OUTSIDE = {
  // ============================================================
  // 背景・フォグ
  // ============================================================
  background: {
    color: '#0c1018', // やや明るい夜空
  },

  fog: {
    color: '#0e1420',
    near: 50,    // ギャラリー外から開始
    far: 200,    // 遠方を溶かす（内装には影響しない距離）
  },

  // ============================================================
  // 雲海レイヤー（床より下、大きく薄く流れる）
  // ============================================================
  clouds: {
    baseY: -12,        // ギャラリー床（y=0）より十分下
    size: 800,         // 非常に大きく端が見えないように

    layers: [
      // 上層（薄い霧、速め）
      { yOffset: 0, opacity: 0.25, speed: 0.002, scale: 1.0, color: '#1e2a3a' },
      // 中層（メイン雲海）
      { yOffset: -3, opacity: 0.35, speed: 0.0015, scale: 1.3, color: '#182230' },
      // 下層（深い雲、遅め）
      { yOffset: -6, opacity: 0.28, speed: 0.001, scale: 1.6, color: '#141c28' },
    ] as const,
  },

  // ============================================================
  // 遠景ライト（水平線付近に集中、上空は極少）
  // ============================================================
  distantLights: {
    // メインライト（水平線付近 y=0〜15）
    main: {
      count: 150,
      radiusMin: 60,
      radiusMax: 180,
      yMin: -5,
      yMax: 15,
      size: 3.5,
      opacity: 0.85,
      color: '#d0e0f0',
    },
    // 上空の光（極少、y=15〜35）
    upper: {
      count: 15,
      radiusMin: 80,
      radiusMax: 200,
      yMin: 15,
      yMax: 35,
      size: 2.5,
      opacity: 0.5,
      color: '#b0c8e0',
    },
    // ビーコン（大きめ、数個だけ）
    beacons: {
      count: 6,
      radiusMin: 70,
      radiusMax: 150,
      yMin: 0,
      yMax: 20,
      size: 14.0,
      opacity: 1.0,
      color: '#f0f8ff',
    },
  },

  // ============================================================
  // 水平線のグロウ（雲海と空の境目）
  // ============================================================
  horizonGlow: {
    enabled: true,
    y: -8,
    radius: 120,
    width: 600,
    opacity: 0.18,
    color: '#1e3050',
  },
} as const;
